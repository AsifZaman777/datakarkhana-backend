# ── Database Configuration (Leave blank for zero-config Local Desktop SQLite) ───
DATABASE_URL=
# Example cloud: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# ── Supabase Storage Configuration (Dataset Cloud Buckets) ─
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
SUPABASE_STORAGE_BUCKET=datasets

# ── Brevo & Email Configuration ───────────────────────────
BREVO_API_KEY=your_brevo_api_key_here
SMTP_SERVER=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=asifdev777@gmail.com
SMTP_PASSWORD=

# ── Public Support Contacts ───────────────────────────────
SUPPORT_EMAIL=asifdev777@gmail.com
HOTLINE_PHONE=+880 1863443343
SUPPORT_HOURS=24/7 Automated System & Live WhatsApp Assistance

# ── Superadmin Configuration ──────────────────────────────
SUPERADMIN_EMAIL=asifdev777@gmail.com
SUPERADMIN_PASSWORD=admin123
SUPERADMIN_NAME=Asif Zaman (Superadmin)

# ── Frontend & Auth Configuration ─────────────────────────
# Select which URL to use: "local" (uses FRONTEND_LOCAL_URL) or "render" (uses FRONTEND_RENDER_URL)
FRONTEND_MODE=local
FRONTEND_LOCAL_URL=http://localhost:3000
FRONTEND_RENDER_URL=https://your-frontend-domain.vercel.app
# FRONTEND_URL is optional - if set, it overrides both.
# FRONTEND_URL=https://your-frontend-domain.vercel.app
JWT_SECRET=marketingostad_super_secret_cyber_key_999

# ── Payment Gateway Configuration (bKash & Pathao Pay) ────
BKASH_NUMBER=+8801824500704
BKASH_ACCOUNT_TYPE=Personal (Send Money)
PATHAO_NUMBER=+8801824500704
PATHAO_ACCOUNT_TYPE=Personal / Merchant

# ── Server & Cloud Linux Configuration ────────────────────
# PORT=8000 (Render, Railway, Heroku automatically inject $PORT)
# CHROME_BIN=/usr/bin/chromium (Optional: Override path to Chrome/Chromium executable on Linux)

